# Changelog

## 0.18.1
- Продакшен-релиз: чистка при удалении (installer.nsh), серверная настройка, обновление манифеста

## 0.17.3
- Профили `LovePolinka@love` и `admin123@luder` получают Max-подписку: безлимитные запросы и все модели
- Исправлен счётчик использования в профиле (лимит ∞ раньше отображался как NaN)

## 0.17.2
- Рекордер клавиш: запись любых комбинаций нажатиями (буквы, цифры, F1–F24, стрелки, спецклавиши, numpad; Ctrl/Alt/Shift/Win) для главного хоткея и быстрых хоткеев
- Главная горячая клавиша всегда открывает выделение (больше не возвращает чат)
- Понятные сообщения об ошибках вместо технических («Race all failed...» → инструкция)
- Ретраи к API-провайдерам на временные сбои (429/5xx)
- Лог ошибок клиента в userData/ludr-error.log
- Сервер: защита /register токеном AUTH_TOKEN
- Убран небезопасный путь скачивания обновления (только через манифест с проверкой sha256)

## 0.17.1
- Оверлей выделения поднят на самый верхний слой окон (screen-saver), поверх полноэкранных приложений и окон ошибок
- Сервер: gzip/brotli, ETag/304, лимит параллельных скачиваний, rate-limit по IP, SQLite WAL, ротация логов, graceful shutdown, опциональный HTTPS напрямую
- Сервер: защита /register токеном (AUTH_TOKEN), клиент передаёт Bearer-токен
- Клиент: ретраи к API-провайдерам на временные сбои (429/5xx), лог ошибок клиента в userData/ludr-error.log
- Клиент: убран небезопасный обработчик скачивания обновления (теперь только через манифест с проверкой sha256)
- Electron обновлён до 43.x, зависимости без известных уязвимостей
- Деплой: примеры systemd-юнита и nginx-конфига в luder-server/deploy/

## 0.17.0
- CI-сборка на Windows, корректная деинсталляция (очистка реестра и файлов)
- .env исключён из установщика

## Ранние версии
- Автообновление с проверкой sha256, система потоков, темы, быстрые хоткеи, автостарт с согласия, работа в фоне в трее
